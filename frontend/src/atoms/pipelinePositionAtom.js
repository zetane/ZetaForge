import { atom } from "jotai";

export const pipelinePositionAtom = atom({
    // zoom: 1,
    // zoom_max: 5.6,
    // zoom_min: 0.15,
    // zoom_value: 0.03,
    // zoom_last_value: 1,
    precanvas: {
        style: {
            transform: ""
        }
    },
});